{% extends 'base.html.twig' %}

{% block title %}Berufswunsch{% endblock %}

{% block body %}
    <h1>Berufswunsch</h1>

    <table class="table">
        <tbody class="thead-light">
            <tr>
                <th>Name</th>
                <td>{{ berufswunsch.name }}</td>
            </tr>
        </tbody>
    </table>

    <div class="btn-group">
        <a href="{{ path('berufswunsch_index') }}" class="btn btn-primary btn-sm" role="button" aria-pressed="true">zurück zur Liste</a>
        <a href="{{ path('berufswunsch_edit', {'id': berufswunsch.id}) }}"  class="btn btn-primary btn-sm" role="button" aria-pressed="true">ändern</a>
    </div>
{% endblock %}

{% block sidebar %}
    {{ parent() }}
     {{ include('berufswunsch/_delete_form.html.twig') }}
{% endblock %} 