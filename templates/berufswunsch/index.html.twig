{% extends 'base.html.twig' %}

{% block title %}Berufswunsch{% endblock %}

{% block body %}
    <h1>Berufswunsch</h1>

    <table class="table table-hover table-striped">
        <thead class="thead-light">
            <tr>
                <th>Name</th>
                <th>Option</th>
            </tr>
        </thead>
        <tbody>
        {% for berufswunsch in berufswunsches %}
            <tr>
                <td>{{ berufswunsch.name }}</td>
                <td>
                    <a href="{{ path('berufswunsch_show', {'id': berufswunsch.id}) }}">show</a>
                    <a href="{{ path('berufswunsch_edit', {'id': berufswunsch.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">keine Einträge gefunden</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('berufswunsch_new') }}" class="btn btn-primary btn-sm active" role="button"><i class="fa fa-plus"></i> neu erstellen</a>
{% endblock %}
