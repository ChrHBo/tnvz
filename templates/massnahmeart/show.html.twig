{% extends 'base.html.twig' %}

{% block title %}Massnahmeart{% endblock %}

{% block body %}
    <h1>Massnahmeart</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ massnahmeart.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ massnahmeart.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('massnahmeart_index') }}">back to list</a>

    <a href="{{ path('massnahmeart_edit', {'id': massnahmeart.id}) }}">edit</a>

    {{ include('massnahmeart/_delete_form.html.twig') }}
{% endblock %}
