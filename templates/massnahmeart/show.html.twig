{% extends 'base.html.twig' %}

{% block title %}Massnahmeart{% endblock %}

{% block body %}
    <h1>Massnahmeart</h1>

    <table class="table">
        <tbody class="thead-light">
            <tr>
                <th>Name</th>
                <td>{{ massnahmeart.name }}</td>
            </tr>
        </tbody>
    </table>
   <div class="btn-group">
    <a href="{{ path('massnahmeart_index') }}" class="btn btn-primary btn-sm" role="button" aria-pressed="true">zurück zur Liste</a>
    <a href="{{ path('massnahmeart_edit', {'id': massnahmeart.id}) }}" class="btn btn-primary btn-sm" role="button" aria-pressed="true">ändern</a>
   </div>

{% endblock %}

{% block sidebar %}
    {{ parent() }}
       {{ include('massnahmeart/_delete_form.html.twig') }}
{% endblock %} 