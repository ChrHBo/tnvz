{% extends 'base.html.twig' %}

{% block title %}Massnahmeart{% endblock %}

{% block body %}
    <h1>Massnahmeart</h1>

    <table class="table table-hover table-striped">
        <thead class="thead-light">
            <tr>
                <th>Name</th>
                <th>Option</th>
            </tr>
        </thead>
        <tbody>
        {% for massnahmeart in massnahmearts %}
            <tr>
                <td>{{ massnahmeart.name }}</td>
                <td>
                    <a href="{{ path('massnahmeart_show', {'id': massnahmeart.id}) }}" class="btn btn-secondary btn-sm active" role="button"><i class="fa fa-eye"></i> anzeigen</a>
                    <a href="{{ path('massnahmeart_edit', {'id': massnahmeart.id}) }}" class="btn btn-secondary btn-sm active" role="button"><i class="fa fa-edit"></i> ändern</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">keine Einträge gefunden</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('massnahmeart_new') }}" class="btn btn-primary btn-sm active" role="button"><i class="fa fa-plus"></i> neu erstellen</a>
{% endblock %}
