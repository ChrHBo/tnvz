{% extends 'base.html.twig' %}

{% block title %}Massnahme{% endblock %}

{% block body %}
    <h1>Massnahme</h1>

    <table class="table">
        <tbody class="thead-light">
            <tr>
                <th>Art</th>
                <td> {{ massnahme.massnahmeart.name }}</td>
            </tr>
            <tr>
                <th>Beginn</th>
                <td>{{ massnahme.beginn ? massnahme.beginn|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Ende</th>
                <td>{{ massnahme.ende ? massnahme.ende|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('massnahme_edit', {'id': massnahme.id}) }}" class="btn btn-primary btn-sm" role="button" aria-pressed="true">ändern</a>

{% endblock %}

{% block sidebar %}
    {{ parent() }}
    {{ include('massnahme/_delete_form.html.twig') }}
   
{% endblock %} 