{% extends 'base.html.twig' %}

{% block title %}Mitarbeiter index{% endblock %}

{% block body %}
    <h1>Mitarbeiter index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Vorname</th>
                <th>Raum</th>
                <th>Fon</th>
                <th>Email</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for mitarbeiter in mitarbeiters %}
            <tr>
                <td>{{ mitarbeiter.id }}</td>
                <td>{{ mitarbeiter.name }}</td>
                <td>{{ mitarbeiter.vorname }}</td>
                <td>{{ mitarbeiter.raum }}</td>
                <td>{{ mitarbeiter.fon }}</td>
                <td>{{ mitarbeiter.email }}</td>
                <td>
                    <a href="{{ path('mitarbeiter_show', {'id': mitarbeiter.id}) }}">show</a>
                    <a href="{{ path('mitarbeiter_edit', {'id': mitarbeiter.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('mitarbeiter_new') }}">Create new</a>
{% endblock %}
