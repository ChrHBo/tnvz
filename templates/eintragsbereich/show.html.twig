{% extends 'base.html.twig' %}

{% block title %}Eintragsbereich{% endblock %}

{% block body %}
    <h1>Eintragsbereich</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ eintragsbereich.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ eintragsbereich.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('eintragsbereich_index') }}">back to list</a>

    <a href="{{ path('eintragsbereich_edit', {'id': eintragsbereich.id}) }}">edit</a>

    {{ include('eintragsbereich/_delete_form.html.twig') }}
{% endblock %}
